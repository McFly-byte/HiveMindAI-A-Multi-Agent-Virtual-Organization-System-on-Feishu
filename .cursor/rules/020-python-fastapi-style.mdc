---
description: "Python + FastAPI 代码风格、类型标注、错误处理和配置规范"
globs:
  - "**/*.py"
alwaysApply: false
---

# Python and FastAPI Style Rules

适用范围：
所有 Python 代码。

基础要求：
1. 使用 Python 3.11+。
2. 所有函数尽量添加类型标注。
3. 所有 Pydantic schema 必须继承 BaseModel。
4. API 入参和出参必须有明确 schema。
5. 不要使用裸 dict 作为复杂业务对象，除非是非常局部的中间变量。
6. 不要写超大函数。单个函数建议控制在 80 行以内。
7. 复杂逻辑应拆成 service / tool / helper。
8. 代码要可读，不要为了炫技写复杂抽象。

FastAPI 规范：
1. 路由定义放在 gateway/routes.py 或对应 router 文件中。
2. app/main.py 只负责创建 FastAPI app、注册 router、初始化日志。
3. 依赖注入放在 app/dependencies.py。
4. 鉴权逻辑放在 gateway/auth.py。
5. /health 逻辑放在 gateway/heartbeat.py。
6. 不要在 route handler 中写复杂业务逻辑。
7. route handler 应调用 AgentRunner 或 service。

配置规范：
1. 所有密钥从环境变量读取。
2. config/settings.py 负责集中读取配置。
3. 不允许在代码中硬编码 app_id、app_secret、app_token、LLM key。
4. .env 不得提交。
5. .env.example 必须保留必要字段。
6. 日志中必须 mask token，只展示前后少量字符或完全隐藏。

错误处理：
1. 捕获异常后必须返回结构化错误。
2. 不允许静默 except。
3. 对外返回错误时不能泄露 token、secret、完整请求头。
4. 飞书 API 错误应转换成项目内部错误类型。
5. Agent 执行失败必须写入 AgentRuns。
6. LLM 输出 JSON 解析失败必须有重试或降级。

测试要求：
1. 每个核心 service/tool 至少提供最小单元测试。
2. 风险规则必须有 test_risk_rules.py。
3. 字段映射必须有 test_field_mapper.py。
4. /health 必须有 test_health.py。
5. Agent 至少有 smoke test，保证能返回 run_id 和结构化结果。

代码风格：
1. 优先使用 early return 减少嵌套。
2. 使用清晰命名，不要使用 a、b、tmp 这类含义不明变量。
3. 业务枚举使用 Literal 或 Enum。
4. 时间字段统一使用 datetime/date。
5. 跨模块导入保持单向依赖，避免循环 import。