---
description: "LLM 调用、Prompt、结构化 JSON 输出和降级策略规范"
globs:
  - "prompts/**/*.md"
  - "tools/llm_tool.py"
  - "tools/followup_generate_tool.py"
  - "tools/weekly_report_generate_tool.py"
  - "agents/followup_agent.py"
  - "agents/weekly_report_agent.py"
  - "agents/risk_analysis_agent.py"
  - "schemas/llm_outputs.py"
alwaysApply: false
---

# LLM and Prompt Rules

LLM 使用边界：
1. 稳定判断优先用规则。
2. LLM 只负责解释、归因、建议、追问生成、周报生成。
3. LLM 不得直接写飞书 Base。
4. LLM 输出必须先经过 Pydantic 校验，再由 Tool 写入 Base。
5. LLM 不得编造不存在的任务、负责人、日期、风险、进展。
6. 如果证据不足，必须输出 need_more_info，而不是强行判断。

必须使用结构化 JSON：
所有 LLM 输出必须要求合法 JSON。
禁止让模型输出 Markdown 后再人工解析。
禁止让模型输出自然语言后直接写入多个字段。

RiskAnalysisAgent 的 LLM 输出应包含：
- risk_title
- risk_type
- risk_level
- trigger_reason
- suggested_action
- need_more_info
- evidence_sources

FollowUpAgent 的 LLM 输出应包含：
- followup_title
- followup_target
- followup_reason
- followup_content

WeeklyReportAgent 的 LLM 输出应包含：
- project_summary
- weekly_progress
- risk_summary
- blockers
- next_week_plan
- decision_suggestions
- pending_decisions

Prompt 编写规则：
1. prompt 必须说明角色。
2. prompt 必须说明输入数据来源于飞书 Base。
3. prompt 必须说明不得编造。
4. prompt 必须列出允许的枚举值。
5. prompt 必须要求输出合法 JSON。
6. prompt 必须说明证据不足时如何处理。
7. prompt 必须尽量短，不要塞入无关背景。
8. prompt 中不要包含 API key、token、secret。

LLMTool 规范：
1. LLMTool 统一管理模型调用。
2. 其他模块不得直接调用具体大模型 SDK。
3. LLMTool 必须支持 timeout。
4. LLMTool 必须支持 retry。
5. LLMTool 必须支持 JSON parse。
6. LLMTool 必须支持 json_repair prompt。
7. LLMTool 失败后必须返回结构化错误。

降级策略：
1. LLM 调用失败时，风险识别仍可使用规则输出基础风险。
2. 追问生成失败时，使用模板生成追问。
3. 周报生成失败时，使用规则统计生成基础周报。
4. LLM JSON 不合法时，修复一次；仍失败则降级。
5. 降级结果必须写入 AgentRuns，说明是 fallback。