---
description: "MVP 架构边界：单体 FastAPI、分层模块、禁止过度设计"
globs: []
alwaysApply: true
---

# MVP Architecture Boundary

本项目采用 MVP 优先架构，不允许过度设计。

推荐架构：
1. 单体 FastAPI 后端；
2. 轻量 Agent Runtime；
3. 四个固定 Agent；
4. Tool Layer 工具层；
5. Feishu Adaptor 飞书适配层；
6. Services 业务服务层；
7. Schemas 数据结构层；
8. Prompts 提示词层；
9. Scripts Demo 脚本；
10. Tests 最小测试。

必须遵守的分层边界：

Gateway 层：
- 负责 API 入口、鉴权、Heartbeat、Cron 预留、Agent 触发、异常捕获。
- 不直接写业务判断逻辑。
- 不直接调用飞书 SDK / OpenAPI。

Agent 层：
- 负责 Agent 的业务流程编排。
- 必须通过 Tool Layer 执行动作。
- 不直接调用飞书 SDK / OpenAPI。
- 不直接访问 .env。
- 不直接拼接 HTTP 请求。

Agent Runtime 层：
- 负责 AgentSession、AgentResult、run_id、统一 Observe/Think/Act/Verify/Log 流程。
- 控制 max_steps。
- 记录 session 中间状态。
- 不写具体业务规则。

Tool Layer：
- 封装所有外部能力和可复用动作。
- Base 读写、LLM 调用、Trace 写入、AgentRuns 写入都必须是 Tool。
- Tool 必须返回统一 ToolResult。
- Tool 不应该包含复杂 Agent 决策逻辑。

Feishu Adaptor：
- 只负责飞书开放平台、多维表格 Base、文档、消息等 API 适配。
- 负责 token 管理、字段映射、table_id 配置、record_id、重试、限流、错误转换。
- 业务逻辑不得写在 adaptor 中。

Services：
- 负责可复用业务逻辑，例如 RiskService、FollowUpService、WeeklyReportService、ProjectHealthService、TraceService、MemoryService。
- Services 可以组合 Tools，但不要绕过分层边界。

Schemas：
- 所有入参、出参、LLM JSON 输出、Base 记录对象都应使用 Pydantic 定义。
- 不允许在多个文件中重复定义同一数据结构。

Prompts：
- 只存放 prompt 模板。
- prompt 必须要求结构化 JSON 输出。
- prompt 不得要求模型编造数据。

禁止行为：
1. 禁止新增复杂微服务。
2. 禁止引入 Celery、Kafka、Redis 等非必要中间件，除非用户明确要求。
3. 禁止写复杂前端作为 MVP 主链路。
4. 禁止用本地 fake 数据替代飞书 Base 核心读写。
5. 禁止让 Agent 直接调用飞书 API。
6. 禁止让 LLM 直接决定写库字段而不经过 Pydantic 校验。
7. 禁止自动修改负责人、排期、优先级等高风险字段；只能写建议。
8. 禁止无限 agent loop。
9. 禁止静默吞异常。
10. 禁止在日志中打印完整 token、app_secret、LLM key。

目录结构必须保持清晰：
- app/
- gateway/
- agents/
- agent_runtime/
- tools/
- adaptors/feishu/
- services/
- schemas/
- prompts/
- config/
- scripts/
- tests/
- traces/
- memory/