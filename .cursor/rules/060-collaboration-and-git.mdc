---
description: "四人协作、Git 分支、提交、PR 和 Cursor 开发流程规范"
globs: []
alwaysApply: true
---

# Collaboration and Git Rules

团队规模：
本项目由 4 人协作开发。

推荐分工：
1. A：架构与集成负责人，负责 Gateway、Agent Runtime、Session、项目骨架、README、集成测试。
2. B：飞书 Base 集成负责人，负责 Feishu Adaptor、字段映射、Base 表、初始化脚本、示例数据。
3. C：规则与风险链路负责人，负责 ProjectSecretaryAgent、RiskAnalysisAgent、RiskRuleTool、RiskService。
4. D：LLM 与 Demo 输出负责人，负责 LLMTool、FollowUpAgent、WeeklyReportAgent、Prompts、Trace、Demo 脚本。

分支规范：
1. 不允许直接在 main 上开发。
2. 每个人从 main 拉自己的 feature 分支。
3. 分支命名建议：
   - feat/lmc-scaffold-runtime
   - feat/xxx-feishu-adaptor
   - feat/xxx-risk-agents
   - feat/xxx-llm-demo
4. 每次开始开发前先同步 main。
5. 合并前必须先解决冲突并跑最小测试。

提交规范：
commit message 使用以下格式：
- chore: 初始化项目骨架
- feat: 新增飞书 Base 读取工具
- feat: 实现项目秘书 Agent 巡检规则
- fix: 修复字段映射错误
- test: 添加风险规则测试
- docs: 更新 README 和 Demo 步骤
- refactor: 调整 AgentRuntime 结构

每次提交前：
1. 检查是否误提交 .env。
2. 检查是否打印 token。
3. 检查是否破坏目录结构。
4. 检查是否绕过分层边界。
5. 至少运行相关 smoke test。

AI Coding 规范：
当使用 Cursor 修改代码时，必须遵守：
1. 先读 README、config/base_tables.yaml、相关 schema，再改代码。
2. 不要一次性大范围重写多个模块。
3. 每次只完成一个明确任务。
4. 修改公共接口前必须说明影响范围。
5. 不要删除别人模块中的 TODO，除非已经实现。
6. 不要私自改表字段名。
7. 不要私自新增复杂依赖。
8. 不要将 mock 作为真实链路。
9. 写完代码后更新对应 README 或注释。
10. 重要修改要补充最小测试。

PR 说明建议包含：
1. 本次修改了哪些模块；
2. 新增了哪些接口或函数；
3. 是否影响 Base 表字段；
4. 是否需要新增环境变量；
5. 如何本地验证；
6. 当前未完成 TODO；
7. 是否存在风险。

合并原则：
1. main 分支必须保持可启动。
2. main 分支必须保留 /health 可用。
3. main 分支不允许出现无法导入的模块。
4. main 分支不允许依赖未提交的本地文件。
5. 合并后必须能运行：
   - pip install -r requirements.txt
   - cp .env.example .env
   - uvicorn app.main:app --reload