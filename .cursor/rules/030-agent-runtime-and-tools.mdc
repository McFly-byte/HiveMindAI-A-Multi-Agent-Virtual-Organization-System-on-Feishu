---
description: "Agent Runtime、Agent Loop、Tool Layer 和 AgentRuns 规范"
globs:
  - "agents/**/*.py"
  - "agent_runtime/**/*.py"
  - "tools/**/*.py"
  - "services/**/*.py"
alwaysApply: false
---

# Agent Runtime and Tool Rules

Agent 运行模型：
所有 Agent 必须遵循固定流程：
1. Observe：读取 Base 数据和上下文；
2. Think：执行规则判断，必要时调用 LLM 解释；
3. Act：调用 Tool 执行写回或生成动作；
4. Verify：校验输出是否合规，确认是否成功写回 Base；
5. Log：写入 AgentRuns 和 Trace。

禁止无限循环：
1. 每个 Agent 必须有 max_steps，默认 5。
2. Agent 必须有明确终止条件。
3. 不要实现开放式自主规划系统。
4. 不要让 Agent 自己随意选择任意工具。
5. MVP 中 Agent 的工具调用路径应尽量固定、可预测。

AgentSession 要求：
每次 Agent 运行必须生成 run_id。
Session 至少包含：
- run_id
- project_id
- agent_name
- trigger_type
- start_time
- max_steps
- input_tables
- input_record_ids
- output_tables
- output_record_ids
- observations
- rule_results
- llm_inputs_summary
- llm_outputs
- tool_results
- errors
- trace_id
- status

业务状态要求：
1. Agent 不得依赖全局变量保存关键业务状态。
2. 关键业务状态必须写回飞书 Base。
3. 本地 session 只保存本次运行上下文和中间摘要。
4. session 结束后必须写 AgentRuns。

ToolResult 规范：
所有 Tool 必须返回 ToolResult：
- tool_name
- success
- inputs_summary
- outputs_summary
- output_record_ids
- error
- retry_count

Tool 调用规则：
1. Agent 只能通过 Tool 读写 Base、调用 LLM、写 Trace。
2. Tool 不应直接决定复杂业务流程。
3. Tool 内部可以调用 service 或 adaptor。
4. BaseQueryTool 只读。
5. BaseCreateTool 和 BaseUpdateTool 是写操作，必须做字段校验。
6. AgentRunLogTool 必须尽量在 finally 中执行。
7. TraceTool 失败不能影响主链路。

AgentRuns 规范：
每个 Agent 执行结束必须写入 AgentRuns，包括：
- 运行 ID
- Agent 名称
- 触发方式
- 输入表
- 输入记录 ID
- 输出表
- 输出记录 ID
- 执行动作
- 判断依据摘要
- 执行状态
- 错误信息
- 开始时间
- 结束时间

幂等要求：
创建风险前必须去重：
project_id + task_id + risk_type + period

创建追问前必须去重：
project_id + task_id + followup_reason + period

创建周报前必须去重：
project_id + period

失败处理：
1. 飞书写入失败：重试有限次数，仍失败则标记 partial_success 或 failed。
2. LLM 失败：重试一次，仍失败使用模板降级。
3. JSON 校验失败：尝试 json_repair，一次失败后降级。
4. 读取记录为空：正常结束，不伪造数据。
5. 证据不足：不要硬判高风险，创建待追问事项。