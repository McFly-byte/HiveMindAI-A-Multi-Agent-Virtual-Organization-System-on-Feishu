---
description: "全局项目背景：飞书 AI 校园挑战赛软件研发 PMO 多 Agent MVP"
globs: []
alwaysApply: true
---

# Project Context

你正在参与开发一个参加【飞书 AI 校园挑战赛】的 MVP 项目。

项目名称：
HiveMindAI / 多维表格上的多智能体虚拟项目办公室。

业务场景：
软件研发项目 PMO 的“虚拟项目办公室”。

核心 Demo 项目：
企业知识库 RAG 系统。

系统定位：
以飞书多维表格 Base 作为唯一业务事实源，以多个 AI Agent 作为虚拟 PMO 员工，围绕软件研发项目过程数据完成：
1. 项目巡检；
2. 风险识别；
3. 追问生成；
4. 追问回复回收；
5. 周报生成；
6. Agent 运行审计。

MVP 目标：
在复赛 Demo 中稳定演示一条真实闭环：
Base 数据创建 -> Agent 读取 -> 异常识别 -> 风险沉淀 -> 追问生成 -> 人工回复 -> 状态回写 -> 周报生成 -> AgentRuns 留痕。

核心约束：
1. 所有关键业务数据必须真实读写飞书多维表格 Base。
2. 禁止用本地 mock 数据替代核心链路。
3. Base 是唯一业务事实源。
4. Agent 之间不得依赖全局变量或私有内存传递关键业务状态。
5. 关键状态必须沉淀到 Base 表中。
6. 每次 Agent 运行必须写 AgentRuns。
7. 所有 Agent 输出必须可追溯、可复现、可解释。
8. 优先保证 5 月 7 日复赛 Demo 可运行、可演示、可回放。
9. 不要设计完整 Jira / Tapd / 禅道替代品。
10. 不要做复杂微服务、复杂权限、多租户、复杂审批流、复杂前端。

核心 Base 表：
1. Projects
2. Tasks
3. Milestones
4. Risks
5. FollowUps
6. WeeklyReports
7. AgentRuns

核心 Agent：
1. ProjectSecretaryAgent：项目秘书 Agent，负责巡检和异常标记。
2. RiskAnalysisAgent：风险识别 Agent，负责风险定级、归因、建议动作。
3. FollowUpAgent：追问 Agent，负责生成追问和回收人工回复。
4. WeeklyReportAgent：周报 Agent，负责生成管理层可读周报。

技术栈默认：
1. Python 3.11+
2. FastAPI
3. Pydantic
4. 飞书多维表格 OpenAPI / SDK
5. 国内大模型 API
6. 本地 JSONL TraceFallback
7. 可选 LangSmith Trace

回答和代码生成时必须优先考虑 MVP：
1. 能跑通优先于架构完整。
2. 真实 API 优先于本地模拟。
3. 明确边界优先于功能堆砌。
4. 稳定 Demo 优先于高级技术名词。